/**
 * @file hooks/index.ts
 * @description Barrel export for all global custom hooks.
 * Feature-scoped hooks live inside their feature directory.
 */

// Global hooks will be exported here as they are created.
// Example: export { useMediaQuery } from "./useMediaQuery"
